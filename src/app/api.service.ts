import { HttpClient } from '@angular/common/http';
import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root'
})
export class ApiService {
/* 
  constructor(private http: HttpClient) { }

  postService(servicesObj: any) {
    return this.http.post<any>(`https://localhost:44314/Employee/add`, servicesObj) //Create a method to return an observable inside the services
  }
   */
}
